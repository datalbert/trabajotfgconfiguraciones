server:
  port: 8070

eureka:
  instance:
    hostname: eurakaserver-container
  client:
    fetchRegistry: false
    registerWithEureka: false
    serviceUrl:
      defaultZone: http://eurekaserver:8070/eureka/
